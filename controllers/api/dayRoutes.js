const router = require('express').Router();
const { User, Day } = require('../../models');

// router.post('/', async (req, res) => {
//     try{
//         const challengeData = await Day.create({
//             ...req.body,
//         })
//     }
// })

module.exports = router;